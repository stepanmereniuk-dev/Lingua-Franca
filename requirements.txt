flask==3.1.3
langdetect

# Mac (Apple Silicon) backend
mlx-lm; sys_platform == "darwin"

# Linux/Windows backend (NVIDIA GPU via CUDA, or CPU)
torch; sys_platform != "darwin"
transformers; sys_platform != "darwin"
accelerate; sys_platform != "darwin"
bitsandbytes; sys_platform != "darwin"
